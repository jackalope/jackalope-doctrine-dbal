language: php

php:
  - 5.3
  - 5.4

env:
  - DB=mysql
  - DB=pgsql

before_script:
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar install --dev
  - ./tests/travis_$DB.sh

script: phpunit -c tests/phpunit_$DB.xml.dist

notifications:
  irc: "irc.freenode.org#jackalope"
